import { SxProps, Theme } from "@mui/material";

/**
 * Header component styles
 */

/** Container styles for the data selector dropdown - adds left margin */
export const dataSelectorContainerStyles: SxProps<Theme> = {
  ml: 2,
};

/** Flexible spacer to push theme toggle to the right */
export const spacerStyles: SxProps<Theme> = {
  flexGrow: 1,
};
