"use client";

import { Typography } from "@mui/material";

/**
 * Title - Application title component.
 * Displays the main heading "SpaceX Launch Data" in the header.
 */
export const Title = () => {
  return (
    <Typography variant="h4" component="h1" fontWeight="bold">
      SpaceX Launch Data
    </Typography>
  );
};
