// Enum for all supported data types
export enum DataType {
  LAUNCHES = "launches",
  ROCKETS = "rockets",
  SHIPS = "ships",
  MISSIONS = "missions",
  DRAGONS = "dragons",
  CAPSULES = "capsules",
}
